# My first bio

Разработал собственное онлайн‑био (персональный сайт/портфолио), где представил информацию о себе, навыках и завершённых проектах. В процессе создания полностью сверстал все страницы с семантической разметкой HTML и оформиl внешний вид с помощью CSS (Flexbox, Grid, анимации). Добавил интерактивность на JavaScript: валидацию форм, обработку событий, хранение данных в localStorage и отслеживание состояния анимаций. Практику и фриланс‑заказы выполнял по реальным макетам (Figma/PSD → верстка), учитывая кросс‑браузерность и оптимизацию ресурсов. Использовал систему контроля версий Git для ведения репозитория и внесения правок по отзывам клиентов. Готов дорабатывать и расширять функционал проекта по требованиям.
# New experience
Использовал для себя новые возможности JavaScript

```
const btn = document.querySelector('.theme');
const mainElement = document.querySelector('main');
btn.addEventListener('click', () => {
    btn.classList.toggle('active');
    document.body.classList.toggle('dark-theme');
    const isDarkTheme = document.body.classList.contains('dark-theme');
    localStorage.setItem('theme', isDarkTheme ? 'dark' : 'light');
});

document.addEventListener('DOMContentLoaded', () => {
    const savedTheme = localStorage.getItem('theme');
    if (savedTheme === 'dark') {
        document.body.classList.add('dark-theme');
        btn.classList.add('active');
    }
});

```
Это был первый опыт разработки темной темы
# Work of archetecture
Сделал первую архитектуру сайта
```
project-root/
│
├── 📁 assets/                 # Статические ресурсы
│   ├── 📁 font/               # Файлы шрифтов
│   └── 📁 img/                # Изображения общего назначения
│       └── 📁 jpg/
│
├── 📁 css/                    # Стили проекта
│   ├── base/                  # Базовые стили
│   │   ├── animation.css      # Анимации и ключевые кадры (@keyframes)
│   │   └── scroll.css         # Стили для скролла (например, кастомный scrollbar)
│   │
│   ├── blocks/                # Стили для независимых блоков (БЭМ-подход)
│   │   └── header/            # Стили для конкретного блока "header"
│   │       └── header.css
│   │
│   ├── pages/                 # Стили, специфичные для отдельных страниц/секций
│   │   ├── contact.css        # Стили для секции "Контакты"
│   │   ├── experience.css     # Стили для секции "Опыт работы"
│   │   ├── projects.css       # Стили для секции "Проекты"
│   │   └── skills.css         # Стили для секции "Навыки"
│   │
│   └── theme/                 # Стили, отвечающие за темы оформления
│       ├── dark-theme.css     # Стили для тёмной темы
│       └── index.css          # Главный файл темы (возможно, светлая тема или импорты)
│
├── 📁 js/                     # Логика на JavaScript
│   ├── logic/                 # Модули с основной логикой
│   │   ├── dark-theme.js      # Логика переключения темы ("dark")
│   │   ├── index.js           # Входная точка JS-логики (инициализация)
│   │   └── main.js            # Основной файл с общей логикой приложения
│   └── vendor/                # Сторонние библиотеки и плагины
│
├── 📁 css/vendor/             # Сторонние CSS-библиотеки и сбросные стили
│   ├── normalize.css          # Нормализация стилей для кроссбраузерности
│   └── variables.css          # Файл с CSS-переменными (Custom Properties)
│
├── 📄 index.html              # Главная и, возможно, единственная HTML-страница
└── 📄 README.md               # Документация проекта (этот файл)
```
# Future Outlook
В будущем планирую исправить все баги, связанные с JavaScript-трекингом, и сделать вёрстку адаптивной.